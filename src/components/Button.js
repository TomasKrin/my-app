const Button = ({children}) => {
    // const {title} = props;
    return(
        // <button>{props.title}</button>
        // <button>{title}</button>
        <button>{children}</button>
    )
}

export default Button